<odoo>
    <record id="view_picking_form_inherit" model="ir.ui.view">
        <field name="name">stock.view_picking_form.inherit</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='location_dest_id']" position="after">
                <field name="orden_ruta"/>
                <field name="zona"/>
                <field name="person_id"/>
                <field name="diference_total"/>
                <button name="change_route_moves" type="object" string="Calcular Ruta" attrs="{'invisible':[('orden_ruta','!=',True)]}" class="oe_edit_only oe_highlight"/>
                <button name="calculate_diferences" type="object" string="Calcular Diferencia" attrs="{'invisible':[('orden_ruta','!=',True)]}" class="oe_edit_only oe_highlight"/>
            </xpath>
            <xpath expr="//notebook" position="inside">
                <page string="Diferencia Ruta" attrs="{'invisible':[('orden_ruta','!=',True)]}">
                    <field name="route_moves">
                        <tree editable="bottom">
                            <field name="product_id"/>
                            <field name="charge_qty"/>
                            <field name="return_qty"/>
                            <field name="sale_qty"/>
                            <field name="diference_qty"/>
                            <field name="price"/>
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>